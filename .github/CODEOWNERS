# Default owner for everything. GitHub automatically requests review
# from these owners on every pull request.
*       @lcsabi

# Security-sensitive files: any change here should go through the same
# owner, but we keep them explicitly listed so the intent is obvious.
/SECURITY.md             @lcsabi
/.github/                @lcsabi
/deploy/                 @lcsabi
/internal/api/           @lcsabi
/internal/dnsserver/     @lcsabi
/internal/blocklist/     @lcsabi
